//everything in here runs ONLY on client (in client subdirectory)

var app = angular.module('socially', ['angular-meteor', 'ui.router',
									 'accounts.ui', 'angularUtils.directives.dirPagination']);

